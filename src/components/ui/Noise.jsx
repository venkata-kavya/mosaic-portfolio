import React from "react";

const Noise = () => (
  <div className="fixed inset-0 w-full h-full pointer-events-none z-[9000] opacity-[0.04] bg-[url('https://grainy-gradients.vercel.app/noise.svg')]" />
);

export default Noise;
